<h1>Hey, sign up!</h1>
<% if flash[:alert] %>
  <p><%= flash[:alert] %></p>
<% end %>
<%= form_for @user do |f| %>
  <% if @user.errors.any? %>
    <p><%= pluralize(@user.errors.count, 'error') %>. Please fix.</p>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>
  <div>Email<br><%= f.email_field :email, placeholder: "e.g. user@test.com", required: true %></div>
  <div>Password<br><%= f.password_field :password, required: true %></div>
  <div>Confirm Password<br><%= f.password_field :password_confirmation, required: true %></div><br>
  <div><%= f.submit "Create account" %></div>
<% end %>
